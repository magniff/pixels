[package]
name = "pixui-demo"
description = "Demo application showing off the pixui toolkit. Uses only the public pixui API."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
pixui = { path = "../pixui", features = ["gpu"] }
